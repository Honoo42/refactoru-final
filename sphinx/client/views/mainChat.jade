head
	title Play SPHINX Today!
	link(rel="stylesheet",type="text/css",href="http://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.1.1/css/bootstrap.min.css")
	link(rel="stylesheet",type="text/css",href=".../public/main.css")
body


//- Main Chat Window
template(name="mainBox")
	.chat-container.col-xs-3.col-xs-offset-4
		h2 Chat with some Friends, #{currentUser.username}
		+chatBox
//- Chat box with messages
template(name="revealChat")
	button.btn.btn-info#reveal-chat.col-xs-3.col-xs-offset-4 Click to open chat box!!

template(name="chatBox")
	#messages
		+each messages
			+chatMessage
	textarea#chat-message.col-xs-12
	button.btn.btn-primary#send.col-xs-6 Send Chat
	button.btn.btn-warning#close-chat.col-xs-6 Click to Close
template(name='chatMessage')
	.user-reference
		b {{user}}:
		| {{message}}
	

//- template(name="user")
//- 	.user(class=selected)
//- 		span.name=  name
//- 		span.score= score

template(name="leaderboard")
	.leaderboard-format.col-xs-3.col-xs-offset-4
		h1 Leaderboard
		each users
			if status = true
				li #{username}: #{monstersDefeated}

		//- if user.status.online
		//- 	p Online!
		//- else
		//- 	p not online..

template(name="chat")
	+revealChat
	+mainBox
	+leaderboard



	//- Meteor.presences.find().count() 