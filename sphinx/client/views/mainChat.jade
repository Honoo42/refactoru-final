head
	title Play SPHINX Today!
	link(rel="stylesheet",type="text/css",href="http://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.1.1/css/bootstrap.min.css")
	link(rel="stylesheet",type="text/css",href=".../public/main.css")
body
	+revealChat
	+mainBox
	+leaderboard

//- Main Chat Window
template(name="mainBox")
	.chat-container
		h2 Chat with some Friends, #{currentUser.username}
		+loginButtons
		+chatBox
//- Chat box with messages
template(name="revealChat")
	button.btn.btn-info#reveal-chat Click to open chat box!!

template(name="chatBox")
	#messages
		+each messages
			+chatMessage
	textarea#chat-message
	br 
	button.btn.btn-primary#send Send Chat
	button.btn.btn-warning#close-chat Click to Close Chat
template(name='chatMessage')
	.user-reference
		b {{user}}:
		| {{message}}
	
template(name='leaderboard')
	.leaderboard
		h1 These are your scores
		ul
			each scores
				li #{monsters}

template(name="user")
	.user(class=selected)
		span.name=  name
		span.score= score